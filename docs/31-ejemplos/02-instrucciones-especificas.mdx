---
slug: /ejemplos/instrucciones-especificas
---

import { ChatConversation, UserMessage, AssistantMessage } from '@site/src/components/ChatConversation';

# Instrucciones específicas

## Ejemplo de prompt con rol específico y objetivos claros

<ChatConversation source="/data/chats/specific-role.yaml" />

>**Por qué funciona**:
>- Define el rol (asesoría pedagógica) y el **objetivo** (actividad lista para usar).
>- Obliga a hacer **preguntas de aclaración** antes de escribir la propuesta.
>- Controla la salida con **encabezados fijos** y restricciones (no inventar datos, marcar supuestos).
>- Introduce buenas prácticas de **privacidad** (no usar datos del centro o alumnado).


# Uso de ejemplos de salida (one-shot / few-shot)

Dependiendo de la pregunta, puede ser útil proporcionar al modelo ejemplos de respuesta.

- Zero-shot: No se dan ejemplos, solo la instrucción.
- One-shot: Se incluye un único ejemplo/patrón para guiar.
- Few-shot: Se ofrecen varios ejemplos para mayor claridad.

## One-shot (patrón de formato)

<ChatConversation source="/data/chats/few-shot-one.yaml" />


## Few-shot (varios patrones)


<ChatConversation source="/data/chats/few-shot-multiple.yaml" />

>**Por qué funciona**:
>- Da ejemplos de referencia (patrón de salida estable).
>- Controla el **formato** (tres líneas fijas) y el **nivel**.
>- Reduce ambigüedad: hace la tarea más evaluable.